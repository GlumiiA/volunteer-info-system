1. Разобраться с таблицей `OAuth` провайдеры => (3)
2. хороший стиль создания таблиц(`user` и мн число?)
3. ограничения в тбалице `user`. Какие поля обязательны при регистрации?
4. Справочник городов в БД и Автодополнение на фронтенде (`API Dadata`/`Yandex`) поле `location` в `user` 
5. Как работаю индексы под капотом? 
